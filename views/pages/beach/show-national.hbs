<div class="container">
    <h1>Playas Nacionales</h1>
    <a class="btn btn-dark" href="/beach/crear">Crear playa</a>
    <div class="row">
        {{#each allNationalBeaches}}
        <div class="col-md-4">
            <figure>{{image}}</figure>
            <h3>{{name}}</h3>
            <p>{{city}}, {{country}}</p>
            <p> {{caption}} </p>
            <a class="btn btn-dark" href="/beach/info/{{id}}">+ info</a>
        </div>
        {{/each}}
    </div>
</div>